{
  "schema_version": 1,
  "status": "pending",
  "release_inputs_sha256": "pending",
  "screenshot_path": "docs/qa/latest-implementation.png",
  "screenshot_sha256": "pending",
  "reviewer": "pending",
  "reviewed_at": "pending",
  "notes": "Awaiting a fresh alpha.9 capture. The accepted alpha.8 record was bound to release-input digest dea704a9a8020e176d4b9bef9182a4ae4dc5278726fea332e0b668e2be99f9e1 at commit ad34cd1a152e2e3bcf728e12f74ecb94c65bc695. This release changes the non-QA release inputs, so that digest no longer describes what would ship. It also changes what the capture would show: a window at 1100 pixels or less no longer opens inside a modal review drawer over an inert queue, which is exactly what the 1024-pixel gate frames, so carrying the alpha.8 frame over would ship a picture of a defect. This file is deliberately pending rather than stale-accepted: a Whole-product QA evidence run binds cleanly against a pending sign-off and fails closed against an accepted one whose digest does not match, so leaving the alpha.8 record in place would block the very run that produces the capture needed to replace it. The screenshot named above is still alpha.8's capture until that run lands and a reviewer accepts its replacement."
}
